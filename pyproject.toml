[project]
name = "marketplace-price-compare"
version = "1.0.0"
description = "Сравнение цен Wildberries и Ozon в Telegram: поиск, лучшая цена, watch-подписки на падение цены"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
